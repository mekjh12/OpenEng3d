//-----------------------------------------------------------------------------
// dummy.vert - 더미 버텍스 셰이더
//-----------------------------------------------------------------------------
// Geometry Shader에서 풀스크린 쿼드를 생성하므로
// 버텍스 셰이더는 아무 작업도 하지 않음
//-----------------------------------------------------------------------------
#version 430 core

void main()
{
    // Geometry Shader에서 모든 작업 수행
    gl_Position = vec4(0.0);
}